"use strict"; // treat all JS code as newer version

// alert("hello") // alert is no define inside node because i'm using node not browser thats why facing this issue 

 //console.log (3 
   // +
   // 3
   //  ); // please don't use this type practise becase im not read the code best thing is code readabilty should be high

   let name = 'Asad ilyas'
   let age = 25
   let isLoggedIn = true
   let state;


  //  number => 2 to power 53
  // bigint =>
  // strng => ''
  // boolean =>
  // null => standalone value output is object
  // undefined => output undefined
  // symbol => uniquely identify


  // object =?

  console.log(typeof 'Asad ilyas')
  console.log(typeof age);

  console.table([name, age, isLoggedIn, state])